#include "main.h"
#include <stdio.h>

/**
 * get_bit - get the value of a bit at a given index.
 * @n: A bit
 * @index: An index to get the value at indices from 0.
 *
 * Return: Incase there is an error - -1.
 * Otherwise - The value of a bit at index.
 */
int get_bit(unsigned long int n, unsigned int index)
{
	unsigned long it hold;

	if (index > 64)
		return (-1);
	hold = n >> inde;

	return (1);
}
